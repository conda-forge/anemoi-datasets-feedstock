{% set version = "0.5.36" %}
{% set python_min = "3.11" %}

package:
  name: anemoi-datasets
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/a/anemoi-datasets/anemoi_datasets-{{ version }}.tar.gz
  sha256: 926918540a92123f836dc2e3f2dea1d2c3a402f70a0849145e8bc632e278a925

build:
  entry_points:
    - anemoi-datasets = anemoi.datasets.__main__:main
  noarch: python
  script: "{{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation"
  number: 0

requirements:
  host:
    - python {{ python_min }}.*
    - setuptools >=60
    - setuptools-scm >=8
    - pip
  run:
    - python >={{ python_min }}
    - anemoi-transform >=0.1.27
    - anemoi-utils >=0.5
    - cfunits
    - glom
    - jsonschema
    - numcodecs <0.16
    - numpy
    - pytest >=8.4.1
    - pytest-xdist >=3.7
    - pyyaml
    - ruamel.yaml >=0.16
    - semantic_version
    - tqdm
    - zarr <=2.18.7

test:
  imports:
    - anemoi
  commands:
    - anemoi-datasets --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/ecmwf/anemoi-datasets/
  summary: A package to hold various functions to support training of ML models on ECMWF data.
  doc_url: https://anemoi-datasets.readthedocs.io/
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - tjtg
    - jasonjunweilyu
    - HCookie
